import { HightlightColorDirective } from './hightlight-color.directive';

describe('HightlightColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightColorDirective();
    expect(directive).toBeTruthy();
  });
});
